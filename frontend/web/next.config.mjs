/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone",
  //   experimental: {
  //     outputStandalone: true,
  //     forceSwcTransforms: true,
  //   },
};

export default nextConfig;
